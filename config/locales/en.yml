en:
  refinery:
    plugins:
      smart_pages:
        title: Smart Pages
    smart_pages:
      admin:
        choose_template: choose template
        template: template
        templates:
          standard: standard
          two_columns: two columns
        parts:
          body: Body
          side_body: Side body
          left: Left column
          right: Right column